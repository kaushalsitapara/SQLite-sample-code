<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.newone">

    <application
        android:allowBackup="true"
        android:icon="@drawable/images"
        android:label="Communal Powerpool"
        android:roundIcon="@drawable/images"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".NavigationAdmin"
            android:label="@string/title_activity_navigation_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".NavigationUser" />
        <activity android:name=".ActivityNavigationAdmin" />
        <activity android:name=".AddNGO" />
        <activity android:name=".NGODetails" />
        <activity android:name=".AddEvent" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>